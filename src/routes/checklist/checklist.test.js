// TODO: add tests

const request = require('supertest');

const { keywordsRouter } = require('./keywords.router');

describe('tests for the keywords endpoint', () => {
  it('returns a 200', () => {
    expect(true).toBe(true);
  });
});
