// TODO: add tests

const request = require('supertest');

const { companiesRouter } = require('./companies.router');

describe('tests for the companies endpoint', () => {
  it('returns a 200', () => {
    expect(true).toBe(true);
  });
});
